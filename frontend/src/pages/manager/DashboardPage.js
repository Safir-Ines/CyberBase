// Manager dashboard reuses the CEO dashboard component — same data, manager just has access to more tools in the sidebar.
import CeoDashboard from '../ceo/DashboardPage';
export default CeoDashboard;
